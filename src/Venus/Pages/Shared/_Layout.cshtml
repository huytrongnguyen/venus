<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Venus</title>
    <link rel="icon" href="/favicon.ico" />
    <link rel="stylesheet" href="/rosie.421395f3.css?_dc=@(DateTime.Now.Ticks)" />
    @RenderSection("Styles", required: false)
</head>
<body class="p-2">
    <div class="app d-flex flex-column border rounded-3">
      <header class="navbar navbar-expand-lg px-3 py-0 border-bottom">
        <span class="navbar-brand mb-0 fw-bold">Venus</span>
        <div class="collapse navbar-collapse">
          <!-- Render the navigation partial view, passing the current page -->
          <partial name="_Navigation" model="@(ViewData["ActivePage"]?.ToString() ?? "Index")" />
        </div>
        <Account />
      </header>
      <div class="app-body d-flex flex-row flex-1">
        @RenderBody()
      </div>
    </div>


    <script src="/rosie.js?_dc=@(DateTime.Now.Ticks)"></script>
    @RenderSection("Scripts", required: false)
</body>
</html>
